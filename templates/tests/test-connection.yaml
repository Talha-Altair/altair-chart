apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "altair-chart.fullname" . }}-test-connection"
  labels:
    {{- include "altair-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "altair-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
